---
# Sample Data Exchange Service Acceptance Configuration - Scale Test (100 Acceptances)
# This file demonstrates accepting all Data Exchange service invitations at scale
# Used in Workflow 4: Data Exchange - Accept Invitation (Non-Graphiant Customer)
# Covers all 100 matches from sample_data_exchange_matches_scale.yaml
# All configurations are under 'data_exchange_acceptances' key
# Each acceptance uses human-readable names that will be resolved to IDs by the data exchange manager
# POST to v1/extranets-b2b-peering/consumer/{match_id}
data_exchange_acceptances:
  - customerName: "FinanceBank-001"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-001"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.0/28"                    # Producer service prefix
        tag: "s-scale-001-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.16/28"                    # Producer service prefix
        tag: "s-scale-001-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-001-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.101.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-001"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.101.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.101.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin001@financebank.com", "support001@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-002"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-001"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.0/28"                    # Producer service prefix
        tag: "s-scale-001-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.16/28"                    # Producer service prefix
        tag: "s-scale-001-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-002-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.102.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-002"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.102.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.102.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin002@financebank.com", "support002@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-003"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-001"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.0/28"                    # Producer service prefix
        tag: "s-scale-001-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.16/28"                    # Producer service prefix
        tag: "s-scale-001-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-003-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.103.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-003"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.103.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.103.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin003@financebank.com", "support003@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-004"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-001"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.0/28"                    # Producer service prefix
        tag: "s-scale-001-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.16/28"                    # Producer service prefix
        tag: "s-scale-001-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-004-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.104.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-004"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.104.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.104.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin004@financebank.com", "support004@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-005"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-001"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.0/28"                    # Producer service prefix
        tag: "s-scale-001-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.16/28"                    # Producer service prefix
        tag: "s-scale-001-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-005-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.105.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-005"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.105.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.105.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin005@financebank.com", "support005@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-006"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-002"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.32/28"                    # Producer service prefix
        tag: "s-scale-002-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.48/28"                    # Producer service prefix
        tag: "s-scale-002-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-006-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.106.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-006"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.106.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.106.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin006@financebank.com", "support006@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-007"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-002"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.32/28"                    # Producer service prefix
        tag: "s-scale-002-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.48/28"                    # Producer service prefix
        tag: "s-scale-002-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-007-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.107.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-007"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.107.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.107.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin007@financebank.com", "support007@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-008"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-002"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.32/28"                    # Producer service prefix
        tag: "s-scale-002-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.48/28"                    # Producer service prefix
        tag: "s-scale-002-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-008-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.108.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-008"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.108.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.108.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin008@financebank.com", "support008@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-009"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-002"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.32/28"                    # Producer service prefix
        tag: "s-scale-002-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.48/28"                    # Producer service prefix
        tag: "s-scale-002-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-009-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.109.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-009"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.109.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.109.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin009@financebank.com", "support009@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-010"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-002"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.32/28"                    # Producer service prefix
        tag: "s-scale-002-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.48/28"                    # Producer service prefix
        tag: "s-scale-002-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-010-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.110.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-010"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.110.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.110.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin010@financebank.com", "support010@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-011"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-003"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.64/28"                    # Producer service prefix
        tag: "s-scale-003-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.80/28"                    # Producer service prefix
        tag: "s-scale-003-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-011-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.111.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-011"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.111.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.111.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin011@financebank.com", "support011@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-012"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-003"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.64/28"                    # Producer service prefix
        tag: "s-scale-003-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.80/28"                    # Producer service prefix
        tag: "s-scale-003-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-012-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.112.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-012"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.112.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.112.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin012@financebank.com", "support012@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-013"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-003"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.64/28"                    # Producer service prefix
        tag: "s-scale-003-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.80/28"                    # Producer service prefix
        tag: "s-scale-003-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-013-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.113.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-013"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.113.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.113.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin013@financebank.com", "support013@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-014"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-003"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    nat:
      - prefix: "10.1.1.64/28"                    # Producer service prefix
        tag: "s-scale-003-prefix1"
      - prefix: "10.1.1.80/28"                    # Producer service prefix
        tag: "s-scale-003-prefix2"
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-014-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.114.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-014"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.114.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.114.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin014@financebank.com", "support014@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-015"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-003"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    nat:
      - prefix: "10.1.1.64/28"                    # Producer service prefix
        tag: "s-scale-003-prefix1"
      - prefix: "10.1.1.80/28"                    # Producer service prefix
        tag: "s-scale-003-prefix2"
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-015-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.115.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-015"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.115.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.115.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin015@financebank.com", "support015@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-016"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-004"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.96/28"                    # Producer service prefix
        tag: "s-scale-004-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.112/28"                    # Producer service prefix
        tag: "s-scale-004-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-016-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.116.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-016"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.116.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.116.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin016@financebank.com", "support016@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-017"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-004"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.96/28"                    # Producer service prefix
        tag: "s-scale-004-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.112/28"                    # Producer service prefix
        tag: "s-scale-004-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-017-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.117.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-017"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.117.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.117.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin017@financebank.com", "support017@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-018"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-004"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.96/28"                    # Producer service prefix
        tag: "s-scale-004-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.112/28"                    # Producer service prefix
        tag: "s-scale-004-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-018-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.118.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-018"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.118.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.118.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin018@financebank.com", "support018@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-019"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-004"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.96/28"                    # Producer service prefix
        tag: "s-scale-004-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.112/28"                    # Producer service prefix
        tag: "s-scale-004-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-019-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.119.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-019"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.119.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.119.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin019@financebank.com", "support019@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-020"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-004"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.96/28"                    # Producer service prefix
        tag: "s-scale-004-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.112/28"                    # Producer service prefix
        tag: "s-scale-004-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-020-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.120.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-020"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.120.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.120.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin020@financebank.com", "support020@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-021"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-005"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.128/28"                    # Producer service prefix
        tag: "s-scale-005-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.144/28"                    # Producer service prefix
        tag: "s-scale-005-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-021-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.121.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-021"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.121.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.121.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin021@financebank.com", "support021@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-022"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-005"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.128/28"                    # Producer service prefix
        tag: "s-scale-005-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.144/28"                    # Producer service prefix
        tag: "s-scale-005-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-022-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.122.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-022"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.122.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.122.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin022@financebank.com", "support022@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-023"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-005"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.128/28"                    # Producer service prefix
        tag: "s-scale-005-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.144/28"                    # Producer service prefix
        tag: "s-scale-005-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-023-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.123.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-023"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.123.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.123.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin023@financebank.com", "support023@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-024"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-005"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.128/28"                    # Producer service prefix
        tag: "s-scale-005-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.144/28"                    # Producer service prefix
        tag: "s-scale-005-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-024-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.124.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-024"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.124.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.124.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin024@financebank.com", "support024@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-025"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-005"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.128/28"                    # Producer service prefix
        tag: "s-scale-005-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.144/28"                    # Producer service prefix
        tag: "s-scale-005-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-025-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.125.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-025"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.125.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.125.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin025@financebank.com", "support025@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-026"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-006"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.160/28"                    # Producer service prefix
        tag: "s-scale-006-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.176/28"                    # Producer service prefix
        tag: "s-scale-006-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-026-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.126.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-026"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.126.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.126.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin026@financebank.com", "support026@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-027"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-006"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.160/28"                    # Producer service prefix
        tag: "s-scale-006-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.176/28"                    # Producer service prefix
        tag: "s-scale-006-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-027-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.127.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-027"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.127.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.127.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin027@financebank.com", "support027@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-028"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-006"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.160/28"                    # Producer service prefix
        tag: "s-scale-006-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.176/28"                    # Producer service prefix
        tag: "s-scale-006-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-028-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.128.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-028"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.128.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.128.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin028@financebank.com", "support028@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-029"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-006"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.160/28"                    # Producer service prefix
        tag: "s-scale-006-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.176/28"                    # Producer service prefix
        tag: "s-scale-006-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-029-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.129.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-029"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.129.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.129.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin029@financebank.com", "support029@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-030"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-006"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.160/28"                    # Producer service prefix
        tag: "s-scale-006-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.176/28"                    # Producer service prefix
        tag: "s-scale-006-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-030-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.130.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-030"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.130.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.130.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin030@financebank.com", "support030@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-031"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-007"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.192/28"                    # Producer service prefix
        tag: "s-scale-007-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.208/28"                    # Producer service prefix
        tag: "s-scale-007-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-031-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.131.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-031"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.131.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.131.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin031@financebank.com", "support031@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-032"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-007"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.192/28"                    # Producer service prefix
        tag: "s-scale-007-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.208/28"                    # Producer service prefix
        tag: "s-scale-007-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-032-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.132.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-032"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.132.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.132.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin032@financebank.com", "support032@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-033"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-007"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.192/28"                    # Producer service prefix
        tag: "s-scale-007-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.208/28"                    # Producer service prefix
        tag: "s-scale-007-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-033-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.133.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-033"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.133.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.133.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin033@financebank.com", "support033@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-034"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-007"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.192/28"                    # Producer service prefix
        tag: "s-scale-007-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.208/28"                    # Producer service prefix
        tag: "s-scale-007-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-034-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.134.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-034"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.134.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.134.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin034@financebank.com", "support034@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-035"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-007"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.192/28"                    # Producer service prefix
        tag: "s-scale-007-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.208/28"                    # Producer service prefix
        tag: "s-scale-007-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-035-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.135.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-035"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.135.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.135.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin035@financebank.com", "support035@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-036"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-008"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.224/28"                    # Producer service prefix
        tag: "s-scale-008-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.240/28"                    # Producer service prefix
        tag: "s-scale-008-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-036-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.136.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-036"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.136.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.136.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin036@financebank.com", "support036@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-037"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-008"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.224/28"                    # Producer service prefix
        tag: "s-scale-008-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.240/28"                    # Producer service prefix
        tag: "s-scale-008-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-037-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.137.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-037"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.137.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.137.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin037@financebank.com", "support037@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-038"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-008"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.224/28"                    # Producer service prefix
        tag: "s-scale-008-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.240/28"                    # Producer service prefix
        tag: "s-scale-008-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-038-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.138.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-038"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.138.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.138.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin038@financebank.com", "support038@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-039"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-008"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.224/28"                    # Producer service prefix
        tag: "s-scale-008-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.240/28"                    # Producer service prefix
        tag: "s-scale-008-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-039-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.139.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-039"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.139.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.139.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin039@financebank.com", "support039@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-040"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-008"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.1.224/28"                    # Producer service prefix
        tag: "s-scale-008-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.1.240/28"                    # Producer service prefix
        tag: "s-scale-008-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-040-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.140.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-040"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.140.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.140.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin040@financebank.com", "support040@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-041"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-009"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.0/28"                    # Producer service prefix
        tag: "s-scale-009-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.16/28"                    # Producer service prefix
        tag: "s-scale-009-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-041-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.141.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-041"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.141.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.141.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin041@financebank.com", "support041@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-042"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-009"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.0/28"                    # Producer service prefix
        tag: "s-scale-009-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.16/28"                    # Producer service prefix
        tag: "s-scale-009-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-042-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.142.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-042"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.142.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.142.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin042@financebank.com", "support042@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-043"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-009"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.0/28"                    # Producer service prefix
        tag: "s-scale-009-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.16/28"                    # Producer service prefix
        tag: "s-scale-009-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-043-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.143.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-043"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.143.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.143.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin043@financebank.com", "support043@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-044"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-009"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.0/28"                    # Producer service prefix
        tag: "s-scale-009-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.16/28"                    # Producer service prefix
        tag: "s-scale-009-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-044-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.144.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-044"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.144.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.144.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin044@financebank.com", "support044@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-045"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-009"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.0/28"                    # Producer service prefix
        tag: "s-scale-009-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.16/28"                    # Producer service prefix
        tag: "s-scale-009-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-045-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.145.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-045"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.145.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.145.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin045@financebank.com", "support045@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-046"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-010"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.32/28"                    # Producer service prefix
        tag: "s-scale-010-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.48/28"                    # Producer service prefix
        tag: "s-scale-010-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-046-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.146.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-046"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.146.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.146.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin046@financebank.com", "support046@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-047"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-010"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.32/28"                    # Producer service prefix
        tag: "s-scale-010-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.48/28"                    # Producer service prefix
        tag: "s-scale-010-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-047-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.147.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-047"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.147.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.147.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin047@financebank.com", "support047@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-048"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-010"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.32/28"                    # Producer service prefix
        tag: "s-scale-010-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.48/28"                    # Producer service prefix
        tag: "s-scale-010-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-048-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.148.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-048"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.148.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.148.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin048@financebank.com", "support048@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-049"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-010"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.32/28"                    # Producer service prefix
        tag: "s-scale-010-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.48/28"                    # Producer service prefix
        tag: "s-scale-010-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-049-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.149.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-049"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.149.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.149.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin049@financebank.com", "support049@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-050"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-010"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.32/28"                    # Producer service prefix
        tag: "s-scale-010-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.48/28"                    # Producer service prefix
        tag: "s-scale-010-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-050-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.150.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-050"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.150.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.150.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin050@financebank.com", "support050@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-001"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-011"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.64/28"                    # Producer service prefix
        tag: "s-scale-011-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.80/28"                    # Producer service prefix
        tag: "s-scale-011-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-001-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.101.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-001"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.101.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.101.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin001@financebank.com", "support001@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-002"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-011"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.64/28"                    # Producer service prefix
        tag: "s-scale-011-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.80/28"                    # Producer service prefix
        tag: "s-scale-011-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-002-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.102.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-002"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.102.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.102.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin002@financebank.com", "support002@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-003"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-011"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.64/28"                    # Producer service prefix
        tag: "s-scale-011-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.80/28"                    # Producer service prefix
        tag: "s-scale-011-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-003-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.103.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-003"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.103.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.103.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin003@financebank.com", "support003@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-004"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-011"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.64/28"                    # Producer service prefix
        tag: "s-scale-011-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.80/28"                    # Producer service prefix
        tag: "s-scale-011-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-004-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.104.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-004"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.104.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.104.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin004@financebank.com", "support004@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-005"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-011"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.64/28"                    # Producer service prefix
        tag: "s-scale-011-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.80/28"                    # Producer service prefix
        tag: "s-scale-011-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-005-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.105.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-005"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.105.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.105.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin005@financebank.com", "support005@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-006"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-012"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.96/28"                    # Producer service prefix
        tag: "s-scale-012-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.112/28"                    # Producer service prefix
        tag: "s-scale-012-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-006-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.106.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-006"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.106.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.106.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin006@financebank.com", "support006@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-007"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-012"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.96/28"                    # Producer service prefix
        tag: "s-scale-012-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.112/28"                    # Producer service prefix
        tag: "s-scale-012-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-007-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.107.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-007"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.107.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.107.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin007@financebank.com", "support007@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-008"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-012"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.96/28"                    # Producer service prefix
        tag: "s-scale-012-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.112/28"                    # Producer service prefix
        tag: "s-scale-012-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-008-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.108.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-008"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.108.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.108.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin008@financebank.com", "support008@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-009"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-012"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.96/28"                    # Producer service prefix
        tag: "s-scale-012-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.112/28"                    # Producer service prefix
        tag: "s-scale-012-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-009-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.109.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-009"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.109.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.109.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin009@financebank.com", "support009@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-010"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-012"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.96/28"                    # Producer service prefix
        tag: "s-scale-012-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.112/28"                    # Producer service prefix
        tag: "s-scale-012-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-010-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.110.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-010"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.110.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.110.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin010@financebank.com", "support010@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-011"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-013"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.128/28"                    # Producer service prefix
        tag: "s-scale-013-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.144/28"                    # Producer service prefix
        tag: "s-scale-013-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-011-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.111.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-011"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.111.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.111.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin011@financebank.com", "support011@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-012"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-013"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.128/28"                    # Producer service prefix
        tag: "s-scale-013-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.144/28"                    # Producer service prefix
        tag: "s-scale-013-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-012-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.112.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-012"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.112.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.112.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin012@financebank.com", "support012@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-013"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-013"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.128/28"                    # Producer service prefix
        tag: "s-scale-013-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.144/28"                    # Producer service prefix
        tag: "s-scale-013-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-013-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.113.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-013"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.113.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.113.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin013@financebank.com", "support013@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-014"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-013"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.128/28"                    # Producer service prefix
        tag: "s-scale-013-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.144/28"                    # Producer service prefix
        tag: "s-scale-013-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-014-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.114.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-014"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.114.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.114.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin014@financebank.com", "support014@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-015"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-013"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.128/28"                    # Producer service prefix
        tag: "s-scale-013-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.144/28"                    # Producer service prefix
        tag: "s-scale-013-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-015-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.115.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-015"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.115.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.115.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin015@financebank.com", "support015@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-016"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-014"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.160/28"                    # Producer service prefix
        tag: "s-scale-014-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.176/28"                    # Producer service prefix
        tag: "s-scale-014-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-016-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.116.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-016"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.116.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.116.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin016@financebank.com", "support016@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-017"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-014"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.160/28"                    # Producer service prefix
        tag: "s-scale-014-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.176/28"                    # Producer service prefix
        tag: "s-scale-014-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-017-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.117.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-017"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.117.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.117.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin017@financebank.com", "support017@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-018"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-014"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.160/28"                    # Producer service prefix
        tag: "s-scale-014-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.176/28"                    # Producer service prefix
        tag: "s-scale-014-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-018-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.118.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-018"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.118.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.118.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin018@financebank.com", "support018@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-019"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-014"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.160/28"                    # Producer service prefix
        tag: "s-scale-014-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.176/28"                    # Producer service prefix
        tag: "s-scale-014-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-019-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.119.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-019"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.119.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.119.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin019@financebank.com", "support019@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-020"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-014"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.160/28"                    # Producer service prefix
        tag: "s-scale-014-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.176/28"                    # Producer service prefix
        tag: "s-scale-014-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-020-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.120.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-020"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.120.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.120.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin020@financebank.com", "support020@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-021"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-015"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.192/28"                    # Producer service prefix
        tag: "s-scale-015-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.208/28"                    # Producer service prefix
        tag: "s-scale-015-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-021-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.121.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-021"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.121.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.121.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin021@financebank.com", "support021@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-022"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-015"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.192/28"                    # Producer service prefix
        tag: "s-scale-015-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.208/28"                    # Producer service prefix
        tag: "s-scale-015-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-022-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.122.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-022"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.122.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.122.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin022@financebank.com", "support022@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-023"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-015"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.192/28"                    # Producer service prefix
        tag: "s-scale-015-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.208/28"                    # Producer service prefix
        tag: "s-scale-015-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-023-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.123.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-023"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.123.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.123.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin023@financebank.com", "support023@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-024"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-015"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.192/28"                    # Producer service prefix
        tag: "s-scale-015-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.208/28"                    # Producer service prefix
        tag: "s-scale-015-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-024-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.124.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-024"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.124.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.124.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin024@financebank.com", "support024@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-025"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-015"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.192/28"                    # Producer service prefix
        tag: "s-scale-015-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.208/28"                    # Producer service prefix
        tag: "s-scale-015-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-025-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.125.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-025"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.125.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.125.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin025@financebank.com", "support025@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-026"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-016"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.224/28"                    # Producer service prefix
        tag: "s-scale-016-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.240/28"                    # Producer service prefix
        tag: "s-scale-016-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-026-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.126.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-026"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.126.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.126.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin026@financebank.com", "support026@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-027"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-016"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.224/28"                    # Producer service prefix
        tag: "s-scale-016-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.240/28"                    # Producer service prefix
        tag: "s-scale-016-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-027-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.127.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-027"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.127.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.127.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin027@financebank.com", "support027@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-028"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-016"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.224/28"                    # Producer service prefix
        tag: "s-scale-016-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.240/28"                    # Producer service prefix
        tag: "s-scale-016-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-028-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.128.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-028"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.128.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.128.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin028@financebank.com", "support028@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-029"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-016"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.224/28"                    # Producer service prefix
        tag: "s-scale-016-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.240/28"                    # Producer service prefix
        tag: "s-scale-016-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-029-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.129.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-029"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.129.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.129.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin029@financebank.com", "support029@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-030"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-016"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.2.224/28"                    # Producer service prefix
        tag: "s-scale-016-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.2.240/28"                    # Producer service prefix
        tag: "s-scale-016-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-030-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.130.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-030"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.130.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.130.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin030@financebank.com", "support030@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-031"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-017"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.0/28"                    # Producer service prefix
        tag: "s-scale-017-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.16/28"                    # Producer service prefix
        tag: "s-scale-017-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-031-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.131.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-031"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.131.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.131.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin031@financebank.com", "support031@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-032"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-017"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.0/28"                    # Producer service prefix
        tag: "s-scale-017-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.16/28"                    # Producer service prefix
        tag: "s-scale-017-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-032-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.132.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-032"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.132.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.132.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin032@financebank.com", "support032@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-033"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-017"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.0/28"                    # Producer service prefix
        tag: "s-scale-017-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.16/28"                    # Producer service prefix
        tag: "s-scale-017-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-033-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.133.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-033"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.133.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.133.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin033@financebank.com", "support033@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-034"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-017"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.0/28"                    # Producer service prefix
        tag: "s-scale-017-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.16/28"                    # Producer service prefix
        tag: "s-scale-017-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-034-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.134.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-034"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.134.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.134.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin034@financebank.com", "support034@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-035"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-017"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.0/28"                    # Producer service prefix
        tag: "s-scale-017-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.16/28"                    # Producer service prefix
        tag: "s-scale-017-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-035-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.135.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-035"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.135.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.135.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin035@financebank.com", "support035@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-036"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-018"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.32/28"                    # Producer service prefix
        tag: "s-scale-018-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.48/28"                    # Producer service prefix
        tag: "s-scale-018-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-036-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.136.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-036"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.136.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.136.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin036@financebank.com", "support036@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-037"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-018"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.32/28"                    # Producer service prefix
        tag: "s-scale-018-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.48/28"                    # Producer service prefix
        tag: "s-scale-018-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-037-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.137.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-037"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.137.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.137.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin037@financebank.com", "support037@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-038"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-018"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.32/28"                    # Producer service prefix
        tag: "s-scale-018-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.48/28"                    # Producer service prefix
        tag: "s-scale-018-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-038-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.138.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-038"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.138.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.138.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin038@financebank.com", "support038@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-039"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-018"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.32/28"                    # Producer service prefix
        tag: "s-scale-018-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.48/28"                    # Producer service prefix
        tag: "s-scale-018-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-039-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.139.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-039"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.139.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.189.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin039@financebank.com", "support039@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-040"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-018"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.32/28"                    # Producer service prefix
        tag: "s-scale-018-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.48/28"                    # Producer service prefix
        tag: "s-scale-018-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-040-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.140.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-040"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.140.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.140.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin040@financebank.com", "support040@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-041"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-019"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.64/28"                    # Producer service prefix
        tag: "s-scale-019-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.80/28"                    # Producer service prefix
        tag: "s-scale-019-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-041-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.141.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-041"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.141.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.141.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin041@financebank.com", "support041@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-042"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-019"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.64/28"                    # Producer service prefix
        tag: "s-scale-019-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.80/28"                    # Producer service prefix
        tag: "s-scale-019-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-042-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.142.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-042"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.142.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.192.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin042@financebank.com", "support042@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-043"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-019"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.64/28"                    # Producer service prefix
        tag: "s-scale-019-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.80/28"                    # Producer service prefix
        tag: "s-scale-019-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-043-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.143.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-043"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.143.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.143.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin043@financebank.com", "support043@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-044"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-019"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.64/28"                    # Producer service prefix
        tag: "s-scale-019-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.80/28"                    # Producer service prefix
        tag: "s-scale-019-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-044-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.144.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-044"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.144.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.144.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin044@financebank.com", "support044@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-045"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-019"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.64/28"                    # Producer service prefix
        tag: "s-scale-019-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.80/28"                    # Producer service prefix
        tag: "s-scale-019-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-045-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.145.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-045"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.145.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.145.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin045@financebank.com", "support045@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-046"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-020"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.96/28"                    # Producer service prefix
        tag: "s-scale-020-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.112/28"                    # Producer service prefix
        tag: "s-scale-020-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-046-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.146.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-046"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.146.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.146.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin046@financebank.com", "support046@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-047"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-020"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.96/28"                    # Producer service prefix
        tag: "s-scale-020-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.112/28"                    # Producer service prefix
        tag: "s-scale-020-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-047-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.147.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-047"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.147.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.147.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin047@financebank.com", "support047@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-048"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-020"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.96/28"                    # Producer service prefix
        tag: "s-scale-020-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.112/28"                    # Producer service prefix
        tag: "s-scale-020-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-048-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.148.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-048"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.148.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.148.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin048@financebank.com", "support048@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-049"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-020"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.96/28"                    # Producer service prefix
        tag: "s-scale-020-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.112/28"                    # Producer service prefix
        tag: "s-scale-020-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-049-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.149.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-049"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.149.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.149.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin049@financebank.com", "support049@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
  - customerName: "FinanceBank-050"             # Customer name and service name combination will be used to lookup matchId
    serviceName: "de-service-scale-020"        # Service name (will be used to lookup serviceId)
    # Site Information Configuration (by name)
    siteInformation:
      - sites: ["site-sjc-sdktest"]            # List of site names (will be resolved to site IDs)
        siteLists: []                          # Optional site list names (empty for direct site selection)
    # NAT Configuration (optional)
    # Maps producer service prefixes (optionally NAT them to avoid overlap with customer network)
    nat:
      - prefix: "10.1.3.96/28"                    # Producer service prefix
        tag: "s-scale-020-prefix1"                       # Corresponding tag from service prefixes
      - prefix: "10.1.3.112/28"                    # Producer service prefix
        tag: "s-scale-020-prefix2"                       # Corresponding tag from service prefixes
    # Policy Configuration (by name)
    policy:
      - lanSegment: "FinanceBank-050-lan-segment"  # LAN segment name (will be resolved to ID)
        consumerPrefixes:                      # Customer/Consumer's own prefixes that will be advertised
          - "10.150.0.0/24"                    # Customer's own /24 network
    # Site-to-Site VPN Configuration
    siteToSiteVpn:
      # IPSec Gateway Details
      ipsecGatewayDetails:
        name: "s2s-FinanceBank-050"            # Gateway name
        destinationAddress: "0.0.0.0"          # Customer's external IP
        mtu: null                              # Optional MTU setting
        tcpMss: null                           # Optional TCP MSS setting
        remoteIkePeerIdentity: "10.150.0.0"    # Optional remote IKE peer identity
        ikeInitiator: false                    # IKE initiation mode (false = responder)
        # Tunnel 1 Configuration
        tunnel1:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.100/30"    # Optional Inside IPv4 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::100/126"       # Optional Inside IPv6 subnet for tunnel 1 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 1 (will be auto filled by Graphiant portal APIs)
        # Tunnel 2 Configuration  
        tunnel2:
          localIkePeerIdentity: "0.0.0.0"       # Optional local IKE peer identity
          insideIpv4Cidr: "169.254.0.104/30"    # Optional Inside IPv4 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          insideIpv6Cidr: "fe80::104/126"       # Optional Inside IPv6 subnet for tunnel 2 (will be auto filled by Graphiant portal APIs)
          psk: changemeOrKeepnull               # Optional Preshared key for tunnel 2 (will be auto filled by Graphiant portal APIs)
        # BGP Routing Configuration
        routing:
          static:
            destinationPrefix:
              - "10.150.0.0/24"                    # Customer's own /24 network
        # VPN Profile Selection
        vpnProfile: "vpnprofile-global-test"  # System VPN profile name
      # Selected Region (by name)
      region: "us-central-1 (Chicago)"       # Region name (will be resolved to regionId)
      # Email notifications
      emails: ["admin050@financebank.com", "support050@financebank.com"]       # Email addresses for notifications
    # Global Object Operations (optional)
    globalObjectOps: {}
    # Routing Policy Table (optional)
    routingPolicyTable: []
